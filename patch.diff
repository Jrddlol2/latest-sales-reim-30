--- src/components/shared/ClaimProgressTracker.tsx
+++ src/components/shared/ClaimProgressTracker.tsx
@@ -91,12 +91,12 @@
           </div>
         ) : (
-          <div className="flex items-center">
+          <div className="flex items-start mt-2">
             {flow.map((stage, i) => {
               const isDone = currentIndex > i;
               const isCurrent = currentIndex === i;
               return (
-                <div key={stage} className="flex items-center flex-1 last:flex-none">
-                  <div className="flex flex-col items-center gap-1.5 min-w-0">
+                <div key={stage} className="flex items-start flex-1 last:flex-none relative">
+                  <div className="flex flex-col items-center gap-1.5 min-w-0 flex-1 relative z-10">
                     <div className={`w-7 h-7 rounded-full flex items-center justify-center shrink-0 ${
                       isDone ? 'bg-primary text-white' : isCurrent ? 'bg-primary/20 text-primary ring-2 ring-primary' : 'bg-surface-container-high text-outline'
                     }`}>
@@ -108,8 +108,8 @@
                     </div>
-                    <span className={`text-[12px] text-center leading-tight ${isCurrent ? 'text-primary font-bold' : 'text-outline'}`}>
+                    <span className={`text-[11px] text-center leading-tight ${isCurrent ? 'text-primary font-bold' : 'text-outline'}`}>
                       {STAGE_LABELS[stage]}
                     </span>
                   </div>
                   {i < flow.length - 1 && (
-                    <div className={`h-0.5 flex-1 mx-1 mb-4 ${isDone ? 'bg-primary' : 'bg-surface-container-high'}`} />
+                    <div className={`absolute top-[13px] left-[50%] right-[-50%] h-0.5 ${isDone ? 'bg-primary' : 'bg-surface-container-high'}`} />
                   )}
                 </div>
